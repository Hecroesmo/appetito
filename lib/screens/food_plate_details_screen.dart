import 'package:flutter/material.dart';

class FoodPlateDetails extends StatelessWidget {
  const FoodPlateDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
